<nav class="bg-white/80 backdrop-blur-lg shadow-lg border-b border-slate-200/50 sticky top-0 z-50" data-controller="mobile-menu">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex">
        <div class="flex-shrink-0 flex items-center">
          <div class="flex items-center gap-2">
            <div class="w-8 h-8 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-lg flex items-center justify-center shadow-md">
              <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z" />
              </svg>
            </div>
            <%= link_to "Clínica App", root_path, class: "text-xl font-bold bg-gradient-to-r from-indigo-600 to-purple-600 bg-clip-text text-transparent hover:from-indigo-500 hover:to-purple-500 transition-all" %>
          </div>
        </div>
        <div class="hidden md:ml-8 md:flex md:space-x-1">
          <%= link_to "Dashboard", root_path, class: nav_link_class(controller_name == "dashboard") %>
          <%= link_to "Pacientes", patients_path, class: nav_link_class(controller_name == "patients") %>
          <%= link_to "Agenda", appointments_path, class: nav_link_class(controller_name == "appointments") %>
          <%= link_to "Prontuários", medical_records_path, class: nav_link_class(controller_name == "medical_records") %>
          <%= link_to "Financeiro", invoices_path, class: nav_link_class(controller_name == "invoices") %>
          <% if current_user.admin? %>
            <%= link_to "Profissionais", practitioners_path, class: nav_link_class(controller_name == "practitioners") %>
            <%= link_to "Relatórios", reports_path, class: nav_link_class(controller_name == "reports") %>
          <% end %>
        </div>
      </div>
      <div class="hidden md:flex md:items-center md:gap-4">
        <div class="flex items-center gap-2 px-3 py-1.5 bg-slate-100 rounded-full">
          <div class="w-7 h-7 bg-gradient-to-br from-indigo-400 to-purple-500 rounded-full flex items-center justify-center text-white text-xs font-medium shadow-sm">
            <%= current_user.name.first.upcase %>
          </div>
          <span class="text-sm font-medium text-slate-700"><%= current_user.name.split.first %></span>
          <span class="text-xs px-2 py-0.5 bg-indigo-100 text-indigo-700 rounded-full font-medium"><%= current_user.role.humanize %></span>
        </div>
        <%= button_to session_path, method: :delete, class: "inline-flex items-center gap-1.5 text-sm font-medium text-slate-500 hover:text-red-600 transition-colors cursor-pointer" do %>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
          </svg>
          Sair
        <% end %>
      </div>
      <!-- Mobile menu button -->
      <div class="flex items-center md:hidden">
        <button type="button" data-action="click->mobile-menu#toggle" class="inline-flex items-center justify-center p-2 rounded-lg text-slate-500 hover:text-slate-700 hover:bg-slate-100 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-indigo-500 transition-colors">
          <span class="sr-only">Abrir menu</span>
          <svg class="h-6 w-6" data-mobile-menu-target="iconOpen" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <svg class="h-6 w-6 hidden" data-mobile-menu-target="iconClose" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu -->
  <div class="md:hidden hidden" data-mobile-menu-target="menu">
    <div class="pt-2 pb-3 space-y-1 bg-white border-t border-slate-100">
      <%= link_to "Dashboard", root_path, class: mobile_nav_link_class(controller_name == "dashboard") %>
      <%= link_to "Pacientes", patients_path, class: mobile_nav_link_class(controller_name == "patients") %>
      <%= link_to "Agenda", appointments_path, class: mobile_nav_link_class(controller_name == "appointments") %>
      <%= link_to "Prontuários", medical_records_path, class: mobile_nav_link_class(controller_name == "medical_records") %>
      <%= link_to "Financeiro", invoices_path, class: mobile_nav_link_class(controller_name == "invoices") %>
      <% if current_user.admin? %>
        <%= link_to "Profissionais", practitioners_path, class: mobile_nav_link_class(controller_name == "practitioners") %>
        <%= link_to "Relatórios", reports_path, class: mobile_nav_link_class(controller_name == "reports") %>
      <% end %>
    </div>
    <div class="pt-4 pb-3 border-t border-slate-200 bg-slate-50">
      <div class="flex items-center px-4">
        <div class="w-10 h-10 bg-gradient-to-br from-indigo-400 to-purple-500 rounded-full flex items-center justify-center text-white font-medium shadow-md">
          <%= current_user.name.first.upcase %>
        </div>
        <div class="ml-3">
          <div class="text-base font-medium text-slate-800"><%= current_user.name %></div>
          <div class="text-sm font-medium text-slate-500"><%= current_user.role.humanize %></div>
        </div>
      </div>
      <div class="mt-3 px-2">
        <%= button_to session_path, method: :delete, class: "w-full flex items-center justify-center gap-2 px-4 py-2 text-sm font-medium text-red-600 hover:bg-red-50 rounded-lg transition-colors cursor-pointer" do %>
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
          </svg>
          Sair da conta
        <% end %>
      </div>
    </div>
  </div>
</nav>
